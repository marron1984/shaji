# 社寺まとめサイト 展開プロンプト

このプロンプトを展開先のNext.jsプロジェクトでClaude Codeに渡すと、
記事表示サイト（カテゴリ一覧/記事一覧/記事詳細/SEO/サイトマップ）が構築されます。

## 使い方

展開先プロジェクトでこのプロンプトをそのまま貼り付けるだけでOKです。

---

## プロンプト本文

```
以下の仕様で開運ガイド記事サイトを構築してください。

### データソース

記事は `public/articles/` にカテゴリ別フォルダで格納されています。

フォルダ構造:
public/articles/
  01_風水_インテリア/
    001_部屋のレイアウトで運気を上げる風水術.txt
    002_xxx.txt
  02_お守り_縁起物/
    001_xxx.txt
  ...

命名規則:
- カテゴリフォルダ: `{番号}_{名前1}_{名前2}` → 表示名は「名前1・名前2」
- 記事ファイル: `{番号}_{タイトル}.txt`
- 記事本文の見出し: `■ 見出し` または `## 見出し` で始まる行

### API

以下のAPIエンドポイントを実装:

GET /api/articles                               → カテゴリ一覧
GET /api/articles?category=01_風水_インテリア     → カテゴリ内の記事一覧
GET /api/articles?category=01_風水_インテリア&slug=001 → 記事全文

レスポンス形式はJSON。

### ページ構成

1. /kaiun-guide - 開運ガイドトップ（カテゴリ一覧 + 各カテゴリの記事カード）
2. /kaiun-guide/[category]/[slug] - 記事詳細ページ
   - パンくずリスト: ホーム > 開運ガイド > カテゴリ名 > 記事タイトル
   - 見出し付きセクション表示
   - generateStaticParams で全記事を静的生成

### SEO対策

- 全ページに適切な title / description メタデータ
- generateMetadata で動的生成
- JSON-LD 構造化データ（Article）
- サイトマップ（/sitemap.xml）を自動生成

### デザイン

- Tailwind CSS使用
- カラーテーマ: 朱色(#D14836), 藍色(#2E3A6E), 金色(#C5A03F)
- レスポンシブ対応（モバイルファースト）
- 記事は whitespace-pre-wrap で段落を保持
- 見出しは左ボーダー付きで金色アクセント

### 記事パーサーの仕様

1. 1行目がタイトル（見出し記号なし）→ スキップ
2. `====` 区切り線 → スキップ
3. `■ xxx` または `## xxx` → セクション見出し
4. それ以外 → 本文段落（見出し前はイントロとして「はじめに」セクション）
```
